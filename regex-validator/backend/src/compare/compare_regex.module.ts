import { Compare_regexService } from './compare_regex.service';
import { Module } from '@nestjs/common';

@Module({
  imports: [],
  controllers: [],
  providers: [Compare_regexService],
})
export class Compare_regexModule {}
